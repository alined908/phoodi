import {GET_INVITES, SEND_INVITE, RESPOND_INVITE} from '..constants/action-types'
import axios from 'axios'

export const getUserInvites = () => async dispatch => {
    
}

export const getRoomInvites = (roomURI) => async dispatch => {

}

export const sendInvite = (roomURI, email) => async dispatch => {

}

export const respondInvite = (roomURI, inviteURI) => async dispatch => {

}